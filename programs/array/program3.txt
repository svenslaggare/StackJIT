func main() Int
{
	.locals 1
	
	PUSHINT 4
	NEWARR Int
	STLOC 0

	LDLOC 0
	PUSHINT 0
	PUSHINT 1337
	STELEM Int

	LDLOC 0
	PUSHINT 0
	LDELEM Int
	CALL rt.println(Int)

	LDLOC 0
	LDLEN
	CALL rt.println(Int)

	PUSHINT 0
	RET
}