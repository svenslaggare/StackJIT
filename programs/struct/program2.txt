struct Point
{
	x Int
	y Int
}

member Point::.constructor() Void
{
   RET
}

func main() Int
{
	.locals 1
	
	PUSHINT 4
	NEWARR Ref.Struct.Point
	STLOC 0

	LDLOC 0
	PUSHINT 0
	NEWOBJ Point::.constructor()
	STELEM Ref.Struct.Point

	LDLOC 0
	PUSHINT 1
	NEWOBJ Point::.constructor()
	STELEM Ref.Struct.Point

	LDLOC 0
	PUSHINT 0
	LDELEM Ref.Struct.Point
	PUSHINT 4711
	STFIELD Point::x

	LDLOC 0
	LDLEN
	CALL rt.println(Int)

	LDLOC 0
	PUSHINT 0
	LDELEM Ref.Struct.Point
	LDFIELD Point::x

	RET
}